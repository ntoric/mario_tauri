name: Build Tauri App

on:
  push:
    branches: [main, master]
    tags:
      - "v*"

  pull_request:
    branches: [main, master]

  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # - platform: macos-latest
          #   target: aarch64-apple-darwin
          #   artifact: mac-arm64
          # - platform: macos-13
          #   target: x86_64-apple-darwin
          #   artifact: mac-amd64
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
            artifact: windows
          # - platform: ubuntu-latest
          #   target: x86_64-unknown-linux-gnu
          #   artifact: linux

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev \
            build-essential \
            curl \
            wget \
            file \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev

      - name: Install frontend dependencies
        run: cd frontend && npm ci

      - name: Build Go printer binary
        shell: bash
        run: |
          cd frontend/src-tauri/mario-printer
          if [ "${{ matrix.platform }}" = "windows-latest" ]; then
            GOOS=windows GOARCH=amd64 go build -o ../target/${{ matrix.target }}/release/mario-printer.exe .
          elif [ "${{ matrix.platform }}" = "macos-latest" ] || [ "${{ matrix.platform }}" = "macos-13" ]; then
            if [ "${{ matrix.target }}" = "aarch64-apple-darwin" ]; then
              GOOS=darwin GOARCH=arm64 go build -o ../target/${{ matrix.target }}/release/mario-printer .
            else
              GOOS=darwin GOARCH=amd64 go build -o ../target/${{ matrix.target }}/release/mario-printer .
            fi
          else
            GOOS=linux GOARCH=amd64 go build -o ../target/${{ matrix.target }}/release/mario-printer .
          fi

      - name: Build app
        run: cd frontend && npm run tauri build -- --target ${{ matrix.target }} --features go-printer

      - name: Copy Go printer binary to bundle
        shell: bash
        run: |
          if [ "${{ matrix.platform }}" = "windows-latest" ]; then
            cp frontend/src-tauri/target/${{ matrix.target }}/release/mario-printer.exe frontend/src-tauri/target/${{ matrix.target }}/release/bundle/
          else
            cp frontend/src-tauri/target/${{ matrix.target }}/release/mario-printer frontend/src-tauri/target/${{ matrix.target }}/release/bundle/
          fi

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mario-juicy-${{ matrix.artifact }}
          path: frontend/src-tauri/target/${{ matrix.target }}/release/bundle/
          retention-days: 30

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            mario-juicy-mac-arm64/**/*.dmg
            mario-juicy-mac-amd64/**/*.dmg
            mario-juicy-windows/**/*.exe
            mario-juicy-linux/**/*.AppImage
            mario-juicy-linux/**/*.deb
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}